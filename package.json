{
  "name": "be-insiculous",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild-check.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "verify": "npm run check && npm run build && npm run a11y",
    "a11y": "node scripts/a11y-check.mjs",
    "deploy": "npm run verify && wrangler deploy",
    "sync:fortknight": "node scripts/sync-fortknight.mjs"
  },
  "dependencies": {
    "@fontsource-variable/jetbrains-mono": "^5.2.5",
    "@fontsource-variable/space-grotesk": "^5.2.5",
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@axe-core/playwright": "^4.13.0",
    "playwright": "^1.55.0",
    "typescript": "^5.8.0",
    "wrangler": "4.123.0"
  }
}
