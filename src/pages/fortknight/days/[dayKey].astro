---
import FaceInDevelopment from "../../../components/FaceInDevelopment.astro";
import { DAY_KEY_ORDER } from "../../../lib/shared/fortknight-rules.js";

// Still emits all 14 paths: the route has to keep existing for the same reason the file does.
export function getStaticPaths() {
  return DAY_KEY_ORDER.map((dayKey) => ({ params: { dayKey } }));
}
---
<FaceInDevelopment title="A day" face="fortknight" />
