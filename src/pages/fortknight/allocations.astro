---
// The old /allocations/ page (time by category) lives on the Overview — keep the URL working.
import { facePath } from "../../lib/faces.js";
const target = facePath("fortknight");
---
<html lang="en"><head><meta charset="utf-8" /><meta http-equiv="refresh" content={`0; url=${target}`} /><title>Moved to Overview</title></head><body><h1>Moved to Overview</h1><p>This page moved to the <a href={target}>Overview</a>.</p></body></html>
