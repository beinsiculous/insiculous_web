---
// The BYO-AI chat became the assistant workspace — keep the old URL working.
import { facePath } from "../../lib/faces.js";
const target = facePath("fortknight", "assistant/");
---
<meta charset="utf-8" /><meta http-equiv="refresh" content={`0; url=${target}`} /><title>Moved to Assistant</title><p>Moved to <a href={target}>Assistant</a>.</p>
