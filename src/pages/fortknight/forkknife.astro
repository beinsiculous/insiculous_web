---
// ForkKnife moved from a FortKnight page to its own face (/forkknife/) — keep the URL working.
import { facePath } from "../../lib/faces.js";
const target = facePath("forkknife");
---
<meta charset="utf-8" /><meta http-equiv="refresh" content={`0; url=${target}`} /><title>Moved to ForkKnife</title><p>Moved to <a href={target}>ForkKnife</a>.</p>
