---
// The old /fortnight/ page became the Overview — keep the URL working.
import { facePath } from "../../lib/faces.js";
const target = facePath("fortknight");
---
<meta charset="utf-8" /><meta http-equiv="refresh" content={`0; url=${target}`} /><title>Moved to Overview</title><p>Moved to <a href={target}>Overview</a>.</p>
